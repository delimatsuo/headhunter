{
  "name": "@hh/hh-trajectory-svc",
  "version": "0.1.0",
  "private": true,
  "description": "Headhunter ML trajectory prediction service using ONNX runtime for career path inference.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "clean": "rimraf dist",
    "dev": "ts-node src/index.ts",
    "start": "node dist/index.js",
    "lint": "eslint \"src/**/*.{ts,tsx}\" --max-warnings=0",
    "typecheck": "tsc -p tsconfig.json --pretty",
    "test": "vitest run"
  },
  "dependencies": {
    "@fastify/under-pressure": "^8.5.0",
    "@hh/common": "file:../common",
    "dotenv": "^16.4.5",
    "ioredis": "^5.3.2",
    "onnxruntime-node": "^1.22.0"
  },
  "devDependencies": {
    "@types/ioredis": "^5.0.0",
    "@types/node": "^20.12.7",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.5",
    "vitest": "^4.0.16"
  }
}
